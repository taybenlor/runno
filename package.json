{
  "private": true,
  "name": "make-run",
  "description": "Monorepo for @make-run packages",
  "version": "0.1.0",
  "license": "MIT",
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "prepare": "husky install"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "husky": "^6.0.0",
    "lerna": "^4.0.0",
    "lint-staged": "^11.0.0",
    "prettier": "^2.3.1"
  }
}
